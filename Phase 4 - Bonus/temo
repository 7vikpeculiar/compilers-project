def main(void)
{
  array int a[5].
  a[0] = 20.
  a[1] = 3.
  a[2] = 1.
  a[3] = 11.
  a[4] = 17.
  int c.
  int d.
  int n = 5.
  int swap

  for (c = 0 ; c < n - 1; c+=1)
  {
    for (d = 0 ; d < n - c - 1; d+=1)
    {
      if (a[d] > a[d+1])
      {
        swap   = a[d].
        a[d]   = a[d+1].
        a[d+1] = swap
      }
    }
  }
    for(c = 0; c < n; c+=1)
    {
      print(c, a[c])
    }
  return void.
}
